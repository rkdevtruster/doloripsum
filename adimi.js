let ref = {}; // Creating an empty object

let scaleName = 'large'; // The value to be assigned

ref.scale = scaleName; // Assigning the value to the "scale" property of "ref"

console.log(ref.scale); // Output: "large"
